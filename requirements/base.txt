# Shared dependencies across every environment.
# Pin exact versions (not ranges) so `pip install -r` is 100% reproducible —
# an unpinned `django-environ>=0.14` could silently pull a breaking major
# version months from now.

Django==6.0.7
django-environ==0.14.0

# psycopg2-binary lives here (not in prod.txt) because since Task 4, `dev`
# also talks to a real PostgreSQL container via docker-compose — the SQLite
# fallback in dev.py only applies when running manage.py bare on the host,
# outside Docker.
psycopg2-binary==2.9.12
